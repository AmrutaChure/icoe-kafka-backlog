name: OKR
description: Create a new Objective & Key Results item
labels:
  - 🎯 OKR

body:
  - type: markdown
    attributes:
      value: |

        Enter OKR details here.

  - type: textarea
    id: objective
    attributes:
      label: Objective
      description: "Enter the objective details here."
    validations:
      required: true

  - type: textarea
    id: key_results
    attributes:
      label: Key Results (Optional)
      description: |
        Enter one key result per line using markdown checkboxes if desired.
    validations:
      required: false
